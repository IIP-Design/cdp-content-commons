// import { selectVideoProjectFiles } from '../selectors';

describe( 'selectVideoProjectFiles', () => {
  it( 'Expect to have unit tests specified', () => {
    expect( true ).toEqual( false );
  } );
} );
