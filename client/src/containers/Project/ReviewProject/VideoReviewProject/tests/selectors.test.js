// import { selectVideoReviewProject } from '../selectors';

describe( 'selectVideoReviewProject', () => {
  it( 'Expect to have unit tests specified', () => {
    expect( true ).toEqual( false );
  } );
} );
