// import { selectVideoSupportFiles } from '../selectors';

describe( 'selectVideoSupportFiles', () => {
  it( 'Expect to have unit tests specified', () => {
    expect( true ).toEqual( false );
  } );
} );
