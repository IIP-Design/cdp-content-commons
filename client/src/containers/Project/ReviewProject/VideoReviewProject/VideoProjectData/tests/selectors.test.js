// import { selectVideoProjectData } from '../selectors';

describe( 'selectVideoProjectData', () => {
  it( 'Expect to have unit tests specified', () => {
    expect( true ).toEqual( false );
  } );
} );
