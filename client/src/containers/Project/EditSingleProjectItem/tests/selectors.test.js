// import { selectEditSingleProjectItem } from '../selectors';

describe( 'selectEditSingleProjectItem', () => {
  it( 'Expect to have unit tests specified', () => {
    expect( true ).toEqual( false );
  } );
} );
