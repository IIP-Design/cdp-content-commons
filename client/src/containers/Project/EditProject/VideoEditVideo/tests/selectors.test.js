// import { selectVideoEditVideo } from '../selectors';

describe( 'selectVideoEditVideo', () => {
  it( 'Expect to have unit tests specified', () => {
    expect( true ).toEqual( false );
  } );
} );
